import React, { useState } from 'react';
import {
  BrowserRouter as Router,
  Route,
  Redirect,
  Switch
} from 'react-router-dom';

const CountryWorkComponent = () => {
    return <h1>Select the Country you wish to move to!</h1>;
  }

export default CountryWorkComponent